# All paths are relative to the iteration root directory.

iteration: 2
random_seed: 42

project_root: "../../"

data:
  train: "../../data/raw/train.csv"
  test: "../../data/raw/test.csv"

target_column: "Survived"
task_type: "binary_classification"

split:
  method: "stratified"
  val_ratio: 0.2
  time_column: null
  cutoff: null

hyperparameters:
  n_estimators: 200
  max_depth: 4
  learning_rate: 0.1
  subsample: 0.8
  min_samples_leaf: 10
  random_state: 42

output_paths:
  metrics: "outputs/metrics.json"
  predictions: "outputs/predictions.csv"
  learning_curves: "outputs/learning_curves.json"
  feature_importance: "outputs/feature_importance.json"
  pipeline_metadata: "outputs/pipeline_metadata.json"
  model: "outputs/model/"
  log: "execution/log.txt"
